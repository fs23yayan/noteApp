# Notes App (Vanilla) — Dicoding Notes API v2

## Setup
1. `cd frontend`
2. `npm install`
3. `npm run start-dev`  -> development server (webpack-dev-server)
4. `npm run build` -> production build (dist/)

## Features
- Create, read, delete, archive/unarchive notes via https://notes-api.dicoding.dev/v2
- Loading indicator implemented as a Web Component
- Error handling with alerts
